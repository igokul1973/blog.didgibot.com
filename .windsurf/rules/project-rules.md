---
trigger: always_on
---

1. Use `pnpm` instead of `npm`.
2. If possible, use scripts defined in `package.json` `scripts` section instead of ad-hoc shell command sequences.
3. No explicit `any` types in TypeScript.
