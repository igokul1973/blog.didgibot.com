#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit hooks..."

# Run linting
echo "Running ESLint..."
pnpm run lint || { echo "❌ ESLint failed. Please fix linting errors before committing."; exit 1; }

# Run type checking
echo "Running TypeScript type checking..."
pnpm run check:types || { echo "❌ Type checking failed. Please fix type errors before committing."; exit 1; }

echo "✅ All pre-commit checks passed!"
