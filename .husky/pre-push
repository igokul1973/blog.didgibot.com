echo "🧪 Running pre-push hooks..."

# For now, skip tests since they need migration from Karma to Vitest
# TODO: Re-enable test validation after migrating tests to Vitest
echo "⚠️  Tests temporarily disabled - Karma tests need migration to Vitest"
echo "✅ Pre-push checks completed ( tests will be re-enabled after migration )"
