echo "🧪 Running pre-push hooks..."

echo "▶ Running Vitest unit tests via pnpm test:unit..."
pnpm test:headless

if [ $? -ne 0 ]; then
  echo "❌ Vitest unit tests failed. Push aborted."
  exit 1
fi

echo "✅ Pre-push checks completed successfully"
